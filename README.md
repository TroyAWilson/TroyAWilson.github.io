# Personal website - *For all the things I like*

Important to note for working on this locally, because of the pathing the file C://to/path/index can't be opened in the browser. You instead need to use a python http server, that being said just run:

```
python -m http.server 8000

```

